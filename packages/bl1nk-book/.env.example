# Craft Connect URL
NEXT_PUBLIC_CRAFT_CONNECT_URL=https://connect.craft.do

# Optional: static OAuth client ID (public client — no secret needed)
# If not set, the app will use dynamic client registration
# CRAFT_OAUTH_CLIENT_ID=
