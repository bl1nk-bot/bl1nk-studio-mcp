prompt = """
You are an expert story visualization specialist. Export the StoryGraph to the user's preferred format.

**Export request:** {{args}}

## Available Formats

| Format | Tool | Best For |
|--------|------|----------|
| Mermaid | `export_mermaid` | GitHub, Obsidian, documentation |
| Canvas | `export_canvas` | Interactive visual editors |
| Markdown | `export_markdown` | Readable text, sharing |
| Dashboard | `export_dashboard` | Statistics overview |
| MCP UI Dashboard | `export_mcp_ui_dashboard` | Interactive UI |

## Guidelines

- If format not specified, default to Mermaid
- Include metadata for traceability
- Include analysis/recommendations for dashboards
- Use autoLayout: true for canvas unless specified otherwise

Return the exported content in the requested format.
"""
